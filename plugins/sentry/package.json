{
  "name": "sentry",
  "description": "Sends errors to sentry server",
  "version": "0.0.1",
  "main": "sentry.js",
  "private": true,
  "dependencies": {
    "raven": "0.7.x"
  },
  "plugin": {
    "consumes": [
      "logger"
    ]
  }
}
