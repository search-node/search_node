{
  "name": "Auth",
  "description": "Provides authentication with API-key to the API",
  "version": "0.0.1",
  "main": "auth.js",
  "private": true,
  "dependencies": {
    "express-jwt": "^6.0.0",
    "jsonwebtoken": "^5.0.2"
  },
  "plugin": {
    "provides": [
      "auth"
    ],
    "consumes": [
      "app",
      "logger",
			"apikeys"
    ]
  }
}
